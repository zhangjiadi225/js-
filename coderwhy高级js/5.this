this指向灵活   跟函数所处位置没有关系   跟函数被调用的方式有关系   是否有调用主体


1  显示绑定
    call与apply  改变this指向 

2  隐式绑定
    通过对象调用  指向对象

3  new绑定  (可与bind一起用  优先级高于bind)
    创建一个对象   这个对象会被执行prototype连接  新对象会绑定到函数调用的this上  如果函数没有返回其他对象 表达式会返回这个新对象

4  默认绑定(独立函数调用)
    window  看是否有调用主体
    


特殊
忽略显示绑定 
    如果绑定null  undefined 会指向window